# genai-ts-rules (planned)

**Status:** Not yet published — placeholder for a future Cursor plugin in this marketplace.

## Intent

- TypeScript conventions as `.cursor/rules/*.mdc`
- No subagents or hooks (stack standards only)
- Install alongside **genai-agent-workflow** for TypeScript application repos

## When added

1. Add `rules/` and `.cursor-plugin/plugin.json` in this directory.
2. Register in [`.cursor-plugin/marketplace.json`](../../.cursor-plugin/marketplace.json).
3. See [plugins/README.md](../README.md) for the full add-plugin checklist.

Source material may come from [genai_ts_rules](https://github.com/genai/genai_ts_rules).
